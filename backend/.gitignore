node_modules/
dist/
.env
.env.*
!.env.example
*.log

# Ad-hoc local verification scripts (e.g. against an ephemeral embedded
# Postgres) — never committed; delete after use.
verify-*.ts
verify-*.mjs
.verify-pgdata/

# Local-disk media storage placeholder (media/storage.ts) — dev only.
uploads/
