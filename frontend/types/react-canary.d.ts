/// React's <ViewTransition> ships in the React canary the App Router uses
/// (next docs: "View transitions"), but its types live behind this
/// reference. One reference anywhere in the project enables them.
/// <reference types="react/canary" />
